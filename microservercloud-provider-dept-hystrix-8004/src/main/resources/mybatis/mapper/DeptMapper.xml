<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dwx.springcloud.mapper.DeptMapper">
  <resultMap id="BaseResultMap" type="com.dwx.springcloud.entities.Dept">
    <id column="dept_id" jdbcType="BIGINT" property="deptId" />
    <result column="dept_name" jdbcType="VARCHAR" property="deptName" />
    <result column="db_source" jdbcType="VARCHAR" property="dbSource" />
  </resultMap>
  <sql id="Base_Column_List">
    dept_id, dept_name, db_source
  </sql>

  <!--查询语句-->
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from dept
    where dept_id = #{deptId,jdbcType=BIGINT}
  </select>
  <select id="selectAll" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from dept
  </select>

  <!--删除语句-->
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from dept
    where dept_id = #{deptId,jdbcType=BIGINT}
  </delete>

  <!--插入语句-->
  <insert id="insert" parameterType="com.dwx.springcloud.entities.Dept">
    insert into dept (dept_id, dept_name, db_source)
        values (#{deptId,jdbcType=BIGINT}, #{deptName,jdbcType=VARCHAR}, DATABASE() )
  </insert>
  <insert id="insertSelective" parameterType="com.dwx.springcloud.entities.Dept">
    insert into dept
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="deptId != null">
        dept_id,
      </if>
      <if test="deptName != null">
        dept_name,
      </if>
      DATABASE()
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="deptId != null">
        #{deptId,jdbcType=BIGINT},
      </if>
      <if test="deptName != null">
        #{deptName,jdbcType=VARCHAR},
      </if>
      DATABASE()
    </trim>
  </insert>

  <!--更新语句-->
  <update id="updateByPrimaryKeySelective" parameterType="com.dwx.springcloud.entities.Dept">
    update dept
    <set>
      <if test="deptName != null">
        dept_name = #{deptName,jdbcType=VARCHAR},
      </if>
    </set>
    where dept_id = #{deptId,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.dwx.springcloud.entities.Dept">
    update dept
    set dept_name = #{deptName,jdbcType=VARCHAR},
      db_source = #{dbSource,jdbcType=VARCHAR}
    where dept_id = #{deptId,jdbcType=BIGINT}
  </update>
</mapper>